/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Portfoilio',
  assetPrefix: '/Portfoilio/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
